import * as React from 'react';
import { ReactComponent as ShareIcon } from '../../../assets/icons/Share.svg';
import { IconButton } from '../IconButton';

export const IconButtonPerf = () => <IconButton icon={<ShareIcon />} />;
