import * as React from 'react';
import { RadioButton } from '../';

export const RadioButtonPerf = () => {
  return (
    <RadioButton checked label={'label'} onChange={() => {}} value={'1'} />
  );
};
