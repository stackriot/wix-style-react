import * as React from 'react';
import { TextField } from '../TextField';
import { classes } from './TextFieldExtended.st.css';

export const TextFieldPerf = () => <TextField className={classes.root} />;
