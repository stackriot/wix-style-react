import TimeInputWip from './TimeInputWip';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TimeInputWip);

metadata.exportInfo = {
  path: 'src/TimeInputWip/TimeInputWip.js',
};
