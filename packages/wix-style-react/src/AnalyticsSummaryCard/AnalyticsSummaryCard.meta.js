import AnalyticsSummaryCard from './AnalyticsSummaryCard';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(AnalyticsSummaryCard);

metadata.exportInfo = {
  path: 'src/AnalyticsSummaryCard/AnalyticsSummaryCard.js',
};
