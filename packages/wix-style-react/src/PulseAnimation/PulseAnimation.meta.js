import PulseAnimation from './PulseAnimation';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(PulseAnimation);

metadata.exportInfo = {
  path: 'src/PulseAnimation/PulseAnimation.js',
};
