import AnalyticsLayout from './AnalyticsLayout';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(AnalyticsLayout);

metadata.exportInfo = {
  path: 'src/AnalyticsLayout/AnalyticsLayout.js',
};
