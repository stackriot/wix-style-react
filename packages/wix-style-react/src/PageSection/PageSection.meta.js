import PageSection from './PageSection';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(PageSection);

metadata.exportInfo = {
  path: 'src/PageSection/PageSection.js',
};
