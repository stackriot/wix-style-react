import TrendIndicator from './TrendIndicator';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TrendIndicator);

metadata.exportInfo = {
  path: 'src/TrendIndicator/TrendIndicator.js',
};
