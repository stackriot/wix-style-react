import PageFooter from './PageFooter';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(PageFooter);

metadata.exportInfo = {
  path: 'src/PageFooter/PageFooter.js',
};
