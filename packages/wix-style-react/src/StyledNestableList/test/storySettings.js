import { Category } from '../../../stories/storiesHierarchy';

export const storySettings = {
  category: Category.WIP,
  storyName: 'StyledNestableList',
  dataHook: 'story-styled-nestable-list',
};
