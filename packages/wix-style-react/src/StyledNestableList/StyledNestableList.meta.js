import StyledNestableList from './StyledNestableList';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(StyledNestableList);

metadata.exportInfo = {
  path: 'src/StyledNestableList/StyledNestableList.js',
};
