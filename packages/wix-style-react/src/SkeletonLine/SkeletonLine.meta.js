import SkeletonLine from './SkeletonLine';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SkeletonLine);

metadata.exportInfo = {
  path: 'src/SkeletonLine/SkeletonLine.js',
};
