{
  "plugins": [
    "stylelint-no-unsupported-browser-features"
  ],
  "rules": {
    "plugin/no-unsupported-browser-features": [true, {
      "severity": "warning",
      "browsers": [
        "Chrome >= 76",
        "Safari >= 12",
        "Edge >= 18",
        "Firefox >= 72"
      ]
    }]
  }
}
