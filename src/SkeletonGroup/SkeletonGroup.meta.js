import SkeletonGroup from './SkeletonGroup';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SkeletonGroup);

metadata.exportInfo = {
  path: 'src/SkeletonGroup/SkeletonGroup.js',
};
