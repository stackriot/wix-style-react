import SkeletonCircle from './SkeletonCircle';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SkeletonCircle);

metadata.exportInfo = {
  path: 'src/SkeletonCircle/SkeletonCircle.js',
};
