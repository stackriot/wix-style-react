import AtlasAddressInput from './AtlasAddressInput';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(AtlasAddressInput);

metadata.exportInfo = {
  path: 'src/AtlasAddressInput/AtlasAddressInput.js',
};
